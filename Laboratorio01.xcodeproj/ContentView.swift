import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Laboratorio 01 - Intro Swift")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}